let firstName = null;
let lastName = 'Perez';

console.log(`${firstName || 'No firstName'} ${lastName || 'No lastName' }`)

let isActive = true;

console.log(` ${isActive == true ? 'Activo' : 'Inactivo'}`)